import React, { useState } from 'react';
import './App.css';

function App() {
	const [todos, setTodos] = useState([
		{ text: "Vacuum bedroom" },
		{ text: "Practice using hooks in React" },
		{ text: "Clean bathroom" },
	]);

	return <div></div>
}

export default App;
